title: Metadata
type: object
description: Metadata for an object
x-something: true
properties:
  created_at:
    type: string
    format: date-time
    description: When the object was created
  updated_at:
    type: string
    format: date-time
    description: When the object was updated
  last_accessed_at:
    type: string
    format: date-time
    description: When the object was last accessed
  test:
    $ref: 'http://localhost:8085/some/route/that/doesnt/exist.yaml'
required:
  - created_at
  - updated_at